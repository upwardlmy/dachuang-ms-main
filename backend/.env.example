# 后端可选环境变量示例（复制为 backend/.env 后按需修改）

# 建议固定 SECRET_KEY，避免后端重启后 JWT token 失效
DJANGO_SECRET_KEY=change-me-to-a-long-random-string

# 数据库连接（不设置则使用默认值：localhost:5432 / dachuang_db / postgres / 123456）
DB_NAME=dachuang_db
DB_USER=postgres
DB_PASSWORD=123456
DB_HOST=localhost
DB_PORT=5432

# 上传/日志目录（不设置会默认写入 .local/backend 下）
# DJANGO_MEDIA_ROOT=/absolute/path/to/media
# DJANGO_LOG_DIR=/absolute/path/to/logs

# 默认密码（不设置时，创建/重置密码接口需要显式传参）
DEFAULT_USER_PASSWORD=123456
DEFAULT_RESET_PASSWORD=123456

# 启动模式：gunicorn（默认）或 runserver
# DJANGO_SERVER_MODE=runserver
